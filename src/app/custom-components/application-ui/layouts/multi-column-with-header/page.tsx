import React, { FunctionComponent } from 'react';
import MultiColumn from './_components/multi-column';

export type MultiColumnPageProps = {
  // no props
};

const MultiColumnPage: FunctionComponent<MultiColumnPageProps> = (props) => {
  return <MultiColumn />;
};

export default MultiColumnPage;
